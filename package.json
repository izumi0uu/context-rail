{
  "name": "context-rail",
  "version": "0.1.0",
  "description": "Live, read-only context window visualization for Oh My Pi",
  "type": "module",
  "license": "MIT",
  "author": "izumi0uu",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/izumi0uu/context-rail.git"
  },
  "homepage": "https://github.com/izumi0uu/context-rail#readme",
  "bugs": {
    "url": "https://github.com/izumi0uu/context-rail/issues"
  },
  "bin": {
    "context-rail": "./dist/hub-cli.js"
  },
  "files": [
    "dist",
    "src",
    "web",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "omp": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "npm run typecheck && npm test",
    "hub": "node --experimental-strip-types src/hub-cli.ts",
    "prepare": "npm run build",
    "preview": "node --experimental-strip-types src/preview.ts",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
	"engines": {
		"node": ">=22.6.0"
	},
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^6.0.3"
  }
}
